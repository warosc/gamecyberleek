# Serves a dist/ built by scripts/release.mjs. Building on the workstation keeps Node, npm and
# the source tree off the VPS; the image is nginx plus static files.
FROM nginx:alpine
COPY dist/ /usr/share/nginx/html/
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
HEALTHCHECK --interval=30s --timeout=3s CMD wget -q -O /dev/null http://127.0.0.1/ || exit 1
