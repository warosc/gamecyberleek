services:
  game:
    build: .
    ports: ['5173:5173']
    volumes:
      - .:/app
      - leek_ops_modules:/app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - VITE_DEBUG_GAME=true
volumes:
  leek_ops_modules:
